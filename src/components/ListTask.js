import React, { NotesContext }  from 'react';
import { useContext } from 'react/cjs/react.development';

export const ListTask = () => {
    const {state} = useContext(NotesContext)
    return (
        <div>
            
        </div>
    )
}
